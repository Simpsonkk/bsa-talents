import { type BadgesItem } from '~/bundles/common-data/badges/types/badges-item.js';

// TODO: Replace with DTO when we know its format, delete duplication files with dto in this folder and types folder
type BsaBadgesStepTypes = {
    badges: BadgesItem[];
};

export { type BsaBadgesStepTypes };
